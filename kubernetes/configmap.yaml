apiVersion: v1
kind: ConfigMap
metadata:
  name: log-agent-config
  namespace: default
data:
  LOG_FILE_PATH: "/app/logs.jsonl"
  LOG_API_URL: "http://log-api-service:8001"
  POLL_INTERVAL: "0.5"
  DURATION: "10"
  COHERE_MODEL: "command"
  # Add any other non-sensitive configuration here
